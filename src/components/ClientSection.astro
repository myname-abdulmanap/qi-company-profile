---
// ClientSection.astro
// Import komponens atau fungsi jika diperlukan
---

<section class="client-section">
  <div class="floating-shape shape-1"></div>
  <div class="floating-shape shape-2"></div>

  <div class="section-title fade-in delay-1">
    <h2>Our Clients</h2>
    <p>
      We're proud to partner with these amazing companies in delivering
      exceptional solutions worldwide.
    </p>
  </div>

</section>

<style>
  .client-section {
    padding: 6rem 0;
    background: var(--service-container);
    position: relative;
    overflow: hidden;
  }

  .section-title {
    text-align: center;
    margin-bottom: 40px;
    position: relative;
    z-index: 2;
  }

  .section-title h2 {
    font-size: 2.2rem;
    font-weight: 700;
    margin-bottom: 12px;
    color: var(--heading-color);
    position: relative;
    display: inline-block;
  }

  .section-title h2:after {
    content: "";
    position: absolute;
    width: 50px;
    height: 3px;
    background: linear-gradient(to right, #4338ca, #ec4899);
    bottom: -8px;
    left: 50%;
    transform: translateX(-50%);
    border-radius: 2px;
  }

  .section-title p {
    font-size: 1rem;
    color: #718096;
    max-width: 550px;
    margin: 0 auto;
    line-height: 1.5;
  }

  .client-carousel {
    position: relative;
    height: 100px;
    margin: 20px 0;
    overflow: hidden;
  }

  .client-track {
    position: absolute;
    white-space: nowrap;
    will-change: transform;
    animation: marquee 40s linear infinite;
    width: fit-content;
  }

  .track-reverse {
    animation: marquee-reverse 35s linear infinite;
  }

  .client-track:hover {
    animation-play-state: paused;
  }

  .client-item {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: 160px;
    height: 80px;
    margin: 0 15px;
    padding: 15px;
    background-color: white;
    border-radius: 10px;
    box-shadow: 0 3px 15px rgba(0, 0, 0, 0.05);
    transition: all 0.5s ease;
    opacity: 0;
    transform: translateY(20px) scale(0.95);
  }

  .client-item.visible {
    opacity: 1;
    transform: translateY(0) scale(1);
  }

  .client-item:hover {
    transform: translateY(-5px) scale(1.05);
    box-shadow: 0 8px 20px rgba(0, 0, 0, 0.1);
    z-index: 10;
  }

  .client-item img {
    max-width: 100%;
    max-height: 45px;
    object-fit: contain;
    filter: grayscale(70%);
    transition: filter 0.4s ease;
  }

  .client-item:hover img {
    filter: grayscale(0%);
  }

  .floating-shape {
    position: absolute;
    border-radius: 50%;
    opacity: 0.08;
    z-index: 0;
  }

  .shape-1 {
    top: 10%;
    left: 10%;
    width: 120px;
    height: 120px;
    background: linear-gradient(45deg, #4761ff, #6c63ff);
    animation: float 15s infinite ease-in-out;
  }

  .shape-2 {
    bottom: 20%;
    right: 5%;
    width: 150px;
    height: 150px;
    background: linear-gradient(45deg, #ff6347, #ff8c69);
    animation: float 18s infinite ease-in-out reverse;
  }

  @keyframes marquee {
    0% {
      transform: translateX(0);
    }
    100% {
      transform: translateX(calc(-50% - 15px));
    }
  }

  @keyframes marquee-reverse {
    0% {
      transform: translateX(calc(-50% - 15px));
    }
    100% {
      transform: translateX(0);
    }
  }

  @keyframes float {
    0%, 100% {
      transform: translate(0, 0) rotate(0deg);
    }
    25% {
      transform: translate(-15px, 15px) rotate(4deg);
    }
    50% {
      transform: translate(8px, -20px) rotate(-4deg);
    }
    75% {
      transform: translate(20px, 8px) rotate(6deg);
    }
  }

  /* Improved Responsiveness */
  @media (max-width: 992px) {
    .client-item {
      width: 140px;
      height: 75px;
      margin: 0 12px;
    }

    .section-title h2 {
      font-size: 2rem;
    }
  }

  @media (max-width: 768px) {
    .client-section {
      padding: 50px 10px;
    }

    .client-item {
      width: 120px;
      height: 70px;
      margin: 0 10px;
      padding: 12px;
    }

    .client-carousel {
      height: 90px;
    }

    .section-title h2 {
      font-size: 1.8rem;
    }

    .section-title p {
      font-size: 0.9rem;
    }
  }

  @media (max-width: 576px) {
    .client-item {
      width: 100px;
      height: 60px;
      margin: 0 8px;
      padding: 10px;
    }

    .client-item img {
      max-height: 35px;
    }

    .client-carousel {
      height: 80px;
    }

    .section-title h2 {
      font-size: 1.6rem;
    }
  }
</style>


<script src="/src/components/js/client.js"></script>

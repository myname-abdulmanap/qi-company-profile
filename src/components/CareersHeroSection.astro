<section class="careers-hero">
  <div class="hero-container">
    <div class="category-tag">Career</div>
    <h1 class="hero-title fade-in delay-1">Explore career opportunities</h1>
    <p class="hero-description fade-in delay-2">
      Discover the latest career openings and professional development opportunities
    </p>
  </div>
</section>

<section class="careers-list">
  <div class="careers-container">
    <div id="careers-container" class="careers-grid">
      <!-- Career cards will be dynamically inserted here -->
    </div>

    <div id="pagination" class="pagination">
      <!-- Pagination buttons will be dynamically inserted here -->
    </div>
  </div>
</section>

<script>
  let currentPage = 1;
  const itemsPerPage = 6;
  let careersData = []; // Your careers data will go here
  let totalPages = 1;

  function renderCareers() {
    if (!careersData.length) return;

    totalPages = Math.ceil(careersData.length / itemsPerPage);
    const startIndex = (currentPage - 1) * itemsPerPage;
    const endIndex = startIndex + itemsPerPage;
    const currentCareers = careersData.slice(startIndex, endIndex);

    const careersContainer = document.getElementById('careers-container');
    careersContainer.innerHTML = '';

    currentCareers.forEach((career) => {
      const careerCard = document.createElement('div');
      careerCard.className = 'career-card fade-in';
      careerCard.innerHTML = `
      <div class="career-header">
        <h3 class="career-title">${career.title}</h3>
        <span class="career-type">${career.type}</span>
      </div>
      <div class="career-details">
        <span class="career-department">${career.department}</span>
        <span class="career-location">${career.location}</span>
      </div>
      <button class="apply-btn" onclick="applyForJob(${career.id})">Apply Now</button>
    `;
      careersContainer.appendChild(careerCard);
    });

    renderPagination();
  }

  function renderPagination() {
    const paginationContainer = document.getElementById('pagination');
    paginationContainer.innerHTML = '';

    if (totalPages <= 1) return;

    // Previous button
    const prevBtn = document.createElement('button');
    prevBtn.className = `pagination-btn ${currentPage === 1 ? 'disabled' : ''}`;
    prevBtn.innerHTML = '&larr;';
    prevBtn.onclick = () => {
      if (currentPage > 1) {
        currentPage--;
        renderCareers();
      }
    };
    paginationContainer.appendChild(prevBtn);

    // Page numbers
    for (let i = 1; i <= totalPages; i++) {
      const pageBtn = document.createElement('button');
      pageBtn.className = `pagination-btn ${i === currentPage ? 'active' : ''}`;
      pageBtn.textContent = i;
      pageBtn.onclick = () => {
        currentPage = i;
        renderCareers();
      };
      paginationContainer.appendChild(pageBtn);
    }

    // Next button
    const nextBtn = document.createElement('button');
    nextBtn.className = `pagination-btn ${currentPage === totalPages ? 'disabled' : ''}`;
    nextBtn.innerHTML = '&rarr;';
    nextBtn.onclick = () => {
      if (currentPage < totalPages) {
        currentPage++;
        renderCareers();
      }
    };
    paginationContainer.appendChild(nextBtn);
  }

  // Function to load careers data - replace this with your API call
  function loadCareersData(data) {
    careersData = data;
    currentPage = 1;
    renderCareers();
  }

  // Function to handle job application - customize as needed
  function applyForJob(jobId) {
    // Your apply logic here
    console.log('Apply for job ID:', jobId);
  }

  // Initialize when page loads
  document.addEventListener('DOMContentLoaded', function () {
    // Call your API or load data here
    // loadCareersData(yourDataArray);
  });
</script>

<style>
  .careers-hero {
    background: var(--container-color);
    min-height: 50vh;
    display: flex;
    align-items: center;
    padding: 4rem 1.5rem 2rem;
    position: relative;
  }

  .hero-container {
    max-width: 900px;
    margin: 0 auto;
    text-align: center;
  }

  .category-tag {
    display: inline-block;
    background: rgba(255, 255, 255, 0.2);
    color: rgb(255, 255, 255);
    padding: 0.4rem 0.8rem;
    border-radius: 50px;
    font-size: 0.8rem;
    margin-bottom: 1.5rem;
    font-weight: 500;
  }

  .hero-title {
    font-size: 2.5rem;
    line-height: 1.2;
    margin-bottom: 1rem;
    color: var(--text-header);
    font-weight: 700;
  }

  .hero-description {
    font-size: 1.1rem;
    max-width: 600px;
    margin: 0 auto;
    color: var(--text-header);
    line-height: 1.6;
    opacity: 0.9;
  }

  .careers-list {
    padding: 2rem 1.5rem 4rem;
    background: var(--container-color);
  }

  .careers-container {
    max-width: 1200px;
    margin: 0 auto;
  }

  .careers-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(350px, 1fr));
    gap: 1.5rem;
    margin-bottom: 3rem;
  }

  .career-card {
    background: var(--card-bg, #16213e);
    border: 1px solid var(--border-color, rgba(255, 255, 255, 0.1));
    border-radius: 12px;
    padding: 1.5rem;
    transition: all 0.3s ease;
    position: relative;
    overflow: hidden;
  }

  .career-card:hover {
    transform: translateY(-5px);
    border-color: var(--primary-color, #4f46e5);
    box-shadow: 0 10px 30px rgba(79, 70, 229, 0.2);
  }

  .career-header {
    display: flex;
    justify-content: space-between;
    align-items: flex-start;
    margin-bottom: 1rem;
  }

  .career-title {
    font-size: 1.25rem;
    font-weight: 600;
    color: var(--text-header);
    margin: 0;
    flex: 1;
    padding-right: 1rem;
  }

  .career-type {
    background: var(--primary-color, #4f46e5);
    color: white;
    padding: 0.25rem 0.75rem;
    border-radius: 20px;
    font-size: 0.75rem;
    font-weight: 500;
    white-space: nowrap;
  }

  .career-details {
    display: flex;
    flex-direction: column;
    gap: 0.5rem;
    margin-bottom: 1.5rem;
  }

  .career-department,
  .career-location {
    font-size: 0.9rem;
    color: var(--text-header);
    opacity: 0.8;
  }

  .career-department::before {
    content: '📁 ';
    margin-right: 0.5rem;
  }

  .career-location::before {
    content: '📍 ';
    margin-right: 0.5rem;
  }

  .apply-btn {
    background: var(--primary-color, #4f46e5);
    color: white;
    border: none;
    padding: 0.75rem 1.5rem;
    border-radius: 8px;
    font-weight: 500;
    cursor: pointer;
    transition: all 0.3s ease;
    width: 100%;
  }

  .apply-btn:hover {
    background: var(--secondary-color, #6366f1);
    transform: translateY(-2px);
  }

  .pagination {
    display: flex;
    justify-content: center;
    align-items: center;
    gap: 0.5rem;
  }

  .pagination-btn {
    background: var(--card-bg, #16213e);
    border: 1px solid var(--border-color, rgba(255, 255, 255, 0.1));
    color: var(--text-header);
    padding: 0.75rem 1rem;
    border-radius: 8px;
    cursor: pointer;
    transition: all 0.3s ease;
    font-weight: 500;
    min-width: 44px;
  }

  .pagination-btn:hover:not(.disabled) {
    background: var(--primary-color, #4f46e5);
    border-color: var(--primary-color, #4f46e5);
  }

  .pagination-btn.active {
    background: var(--primary-color, #4f46e5);
    border-color: var(--primary-color, #4f46e5);
  }

  .pagination-btn.disabled {
    opacity: 0.5;
    cursor: not-allowed;
  }

  /* Animations */
  .fade-in {
    opacity: 0;
    transform: translateY(20px);
    animation: fadeIn 0.5s ease forwards;
  }

  @keyframes fadeIn {
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  .delay-1 {
    animation-delay: 0.2s;
  }
  .delay-2 {
    animation-delay: 0.4s;
  }

  /* Responsive Design */
  @media (max-width: 768px) {
    .careers-hero {
      padding: 3rem 1rem 1.5rem;
      min-height: 40vh;
    }

    .hero-title {
      font-size: 2rem;
    }

    .hero-description {
      font-size: 1rem;
    }

    .careers-grid {
      grid-template-columns: 1fr;
      gap: 1rem;
    }

    .career-header {
      flex-direction: column;
      align-items: flex-start;
      gap: 0.5rem;
    }

    .career-title {
      padding-right: 0;
    }

    .pagination-btn {
      padding: 0.5rem 0.75rem;
      min-width: 36px;
    }
  }

  @media (max-width: 480px) {
    .careers-list {
      padding: 1.5rem 1rem 3rem;
    }

    .pagination {
      gap: 0.25rem;
    }

    .pagination-btn {
      padding: 0.5rem;
      min-width: 32px;
      font-size: 0.9rem;
    }
  }
</style>
